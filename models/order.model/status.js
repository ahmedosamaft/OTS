const status = {
    Waiting: "waiting",
    Accepted : "accepted",
    Rejected: "rejected",
    Shipped: "shipped",
    Delivered: "delivered"
}
const statusenum = Object.values(status)
	
export {status, statusenum}